import React from 'react';

import { LoaderContainer, Ball } from './loader.styles';

const Loader = () => {
  return (
    <LoaderContainer>
      <Ball />
    </LoaderContainer>
  );
};

export default Loader;
